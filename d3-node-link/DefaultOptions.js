// DefaultOptions.js
var myApp = myApp || {};

myApp.default_options = (function() {
	var DEFAULT_OPTIONS = { width: 960,
		                    height: 500 };

	return {
		defaults: DEFAULT_OPTIONS
	};
}());
